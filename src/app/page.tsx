// Import the component for creating notes
import NoteCreator from '@/components/NoteCreator';

// This is the main home page of the application
export default function Home() {
  // It renders the NoteCreator component
  return <NoteCreator />;
}
